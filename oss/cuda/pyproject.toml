[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oss_cuda"
version = "0.2.0+phase2a"
description = "OpenSuperSampling custom CUDA extension stubs"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
