[build-system]
requires = ["setuptools>=68", "torch==2.4.1"]
build-backend = "setuptools.build_meta"

[project]
name = "oss_cuda"
version = "0.1.0+phase1"
description = "OpenSuperSampling custom CUDA extension stubs"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
dependencies = ["torch>=2.4,<2.7"]
