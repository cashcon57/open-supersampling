"""Tests for the OSS Capture Tool — server-side ingestion + index builder.

The Codex-side tandem is responsible for ``test_fixtures.py`` and the
``test_e2e.py`` round-trip. Claude implements the unit tests here for
the FastAPI ingest server, the R2 layout, and the daily index builder.
"""
